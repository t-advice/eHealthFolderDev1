<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="eHealthFolderDev.Views.AddAppointmentPage"
             Title="Add Appointment"
             BackgroundColor="White">
    
    <Grid>
        <ScrollView>
            <VerticalStackLayout Padding="25" Spacing="20">
                
                <!-- Progress Indicator -->
                <Grid ColumnDefinitions="*,*,*" ColumnSpacing="10" Margin="0,10,0,20">
                    <BoxView x:Name="Step1Indicator" Grid.Column="0" HeightRequest="4" BackgroundColor="#FF9800" CornerRadius="2"/>
                    <BoxView x:Name="Step2Indicator" Grid.Column="1" HeightRequest="4" BackgroundColor="#E0E0E0" CornerRadius="2"/>
                    <BoxView x:Name="Step3Indicator" Grid.Column="2" HeightRequest="4" BackgroundColor="#E0E0E0" CornerRadius="2"/>
                </Grid>

                <!-- Step Title -->
                <Label x:Name="StepTitle" 
                       Text="Step 1: Location Details"
                       FontSize="24"
                       FontAttributes="Bold"
                       HorizontalOptions="Center"
                       Margin="0,0,0,10"/>

                <!-- Step 1: Location Info -->
                <VerticalStackLayout x:Name="Step1" Spacing="15">
                    <Label Text="Hospital/Clinic" FontSize="16" FontAttributes="Bold"/>
                    <Entry x:Name="HospitalEntry" Placeholder="Enter hospital or clinic name" FontSize="16"/>

                    <Label Text="Location/Address" FontSize="16" FontAttributes="Bold" Margin="0,10,0,0"/>
                    <Entry x:Name="LocationEntry" Placeholder="Enter address or location" FontSize="16"/>
                </VerticalStackLayout>

                <!-- Step 2: Date & Time -->
                <VerticalStackLayout x:Name="Step2" IsVisible="False" Spacing="15">
                    <Label Text="Appointment Date" FontSize="16" FontAttributes="Bold"/>
                    <DatePicker x:Name="DatePicker" Format="dd/MM/yyyy" FontSize="16"/>

                    <Label Text="Appointment Time" FontSize="16" FontAttributes="Bold" Margin="0,10,0,0"/>
                    <TimePicker x:Name="TimePicker" Format="HH:mm" FontSize="16"/>
                </VerticalStackLayout>

                <!-- Step 3: Details -->
                <VerticalStackLayout x:Name="Step3" IsVisible="False" Spacing="15">
                    <Label Text="Reason for Appointment" FontSize="16" FontAttributes="Bold"/>
                    <Editor x:Name="ReasonEditor" 
                            Placeholder="Enter reason for appointment" 
                            HeightRequest="100"
                            FontSize="16"/>

                    <Label Text="Notes" FontSize="16" FontAttributes="Bold" Margin="0,10,0,0"/>
                    <Editor x:Name="NotesEditor" 
                            Placeholder="Additional notes or preparation instructions" 
                            HeightRequest="120"
                            FontSize="16"/>
                </VerticalStackLayout>

                <!-- Navigation Buttons -->
                <Grid ColumnDefinitions="*,*" ColumnSpacing="15" Margin="0,30,0,10">
                    <Button x:Name="BackButton"
                            Grid.Column="0"
                            Text="Back"
                            Clicked="OnBackClicked"
                            BackgroundColor="#F5F5F5"
                            TextColor="#666666"
                            FontSize="16"
                            HeightRequest="50"
                            CornerRadius="10"
                            BorderColor="#CCCCCC"
                            BorderWidth="1"/>
                    
                    <Button x:Name="NextButton"
                            Grid.Column="1"
                            Text="Next"
                            Clicked="OnNextClicked"
                            BackgroundColor="#FF9800"
                            TextColor="White"
                            FontSize="16"
                            FontAttributes="Bold"
                            HeightRequest="50"
                            CornerRadius="10"/>
                </Grid>

            </VerticalStackLayout>
        </ScrollView>

        <!-- Loading Overlay -->
        <Grid x:Name="LoadingOverlay" IsVisible="False" BackgroundColor="#80000000">
            <ActivityIndicator IsRunning="True" Color="White" WidthRequest="50" HeightRequest="50"/>
        </Grid>
    </Grid>
</ContentPage>